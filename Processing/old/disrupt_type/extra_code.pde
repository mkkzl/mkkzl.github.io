//float pixel_ratio=0;
//int is_background=0;
/* Loop through every pixel row
  for (int y_val = 0; y_val < 100; y_val++) {
    // Loop through every pixel column
    for (int x_val = 0; x_val < 100; x_val++) {
      // Use the formula to find the 1D location
      //int loc = x_val + y_val * width;
      int pixel_color = get(x_val, y_val);
      
      println(pixel_color);
      
      if (pixel_color== 0){
        is_background+=1;
      }
      
      
    }
  }
  pixel_ratio= is_background/(width*height);
  if(pixel_ratio > .1){
    background(0);
  }*/
